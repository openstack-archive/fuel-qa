- id: custom_task_on_controller
  type: shell
  version: 2.0.0
  role: ['/(primary-)?controller/']
  parameters:
    cmd: 'echo "controller task executed" > /tmp/controller_custom_task'

- id: custom_task_on_compute
  type: shell
  version: 2.0.0
  role: ['compute']
  parameters:
    cmd: 'echo "compute task executed" > /tmp/compute_custom_task'

- id: custom_task_on_cinder
  type: shell
  version: 2.0.0
  role: ['cinder']
  parameters:
    cmd: 'echo "cinder task executed" > /tmp/cinder_custom_task'

- id: custom_task_on_ceph-osd
  type: shell
  version: 2.0.0
  role: ['ceph-osd']
  parameters:
    cmd: 'echo "ceph-osd task executed" > /tmp/ceph-osd_custom_task'
