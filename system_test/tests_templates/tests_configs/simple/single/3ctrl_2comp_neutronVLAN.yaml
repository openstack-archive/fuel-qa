---
template:
 name: 3 Controller, 2 Compute on Neutron/VLAN
 slaves: 5
 cluster_template:
  name: SimpleNeutronVLAN
  release: ubuntu
  network:
   !include cluster_configs/networks/neutron_vlan.yaml
  settings:
   !include cluster_configs/settings/cinder_wo_components.yaml
  nodes:
   !include cluster_configs/nodes/single/3ctrl_2comp.yaml